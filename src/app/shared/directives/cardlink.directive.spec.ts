import { CardlinkDirective } from './cardlink.directive';

describe('CardlinkDirective', () => {
  it('should create an instance', () => {
    const directive = new CardlinkDirective();
    expect(directive).toBeTruthy();
  });
});

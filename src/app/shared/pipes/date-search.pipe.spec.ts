import { DateSearchPipe } from './sort-date.pipe';

describe('DateSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new DateSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
